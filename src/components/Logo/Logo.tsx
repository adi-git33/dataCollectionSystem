import IconButton from "@mui/material/IconButton";
import AdsClickIcon from "@mui/icons-material/AdsClick";

const Logo = () => {
  return (
    <div>
      <IconButton>
        <AdsClickIcon />
      </IconButton>
    </div>
  );
};
export default Logo;
